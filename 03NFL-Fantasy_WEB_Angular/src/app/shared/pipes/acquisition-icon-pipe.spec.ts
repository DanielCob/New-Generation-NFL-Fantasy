import { AcquisitionIconPipe } from './acquisition-icon-pipe';

describe('AcquisitionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AcquisitionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
