import { InjuryStatusClassPipe } from './injury-status-class-pipe';

describe('InjuryStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new InjuryStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
