import { PositionIconPipe } from './position-icon-pipe';

describe('PositionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
